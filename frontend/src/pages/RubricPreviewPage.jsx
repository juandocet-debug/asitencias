import React from 'react';
import { ArrowLeft, Download } from 'lucide-react';

export default function RubricPreviewPage({ rubric, onBack }) {
    const grade = rubric?.studentGrade;
    const scores = grade?.scores || {};
    return <div className="mx-auto max-w-6xl space-y-4 pb-8"><header className="flex flex-wrap items-center gap-3"><button onClick={onBack} className="icon-soft"><ArrowLeft size={19} /></button><div className="min-w-0 flex-1"><p className="text-xs font-black uppercase tracking-widest text-cyan-700">Vista previa para estudiantes</p><h1 className="text-xl font-black text-slate-900">{rubric.title}</h1><p className="text-sm font-semibold text-slate-500">Así verá el instrumento y el resultado el estudiante.</p></div>{grade && <div className="rounded-xl bg-cyan-50 px-4 py-2 text-center"><p className="text-[10px] font-black uppercase text-cyan-700">Nota final</p><p className="text-xl font-black text-cyan-800">{grade.final_grade}</p></div>}<button onClick={() => window.print()} className="inline-flex items-center gap-2 rounded-lg bg-cyan-700 px-3 py-2 text-sm font-black text-white"><Download size={15} /> Descargar PDF</button></header><main className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm sm:p-6"><h2 className="text-lg font-black text-slate-900">{rubric.title}</h2>{rubric.description && <p className="mt-2 text-sm text-slate-600">{rubric.description}</p>}<div className="mt-5 grid gap-4">{(rubric.criteria || []).map((criterion, index) => <section key={criterion.id} className="overflow-hidden rounded-xl border border-slate-200"><div className="flex items-center gap-2 bg-slate-100 px-3 py-2"><span className="grid h-6 w-6 place-items-center rounded-full bg-cyan-700 text-xs font-black text-white">{index + 1}</span><h3 className="font-black text-slate-800">{criterion.name}</h3>{scores[criterion.id] && <span className="ml-auto rounded-full bg-cyan-100 px-2 py-1 text-xs font-black text-cyan-800">Nivel {scores[criterion.id]}</span>}</div><div className="grid divide-y divide-slate-200 sm:grid-cols-5 sm:divide-x sm:divide-y-0">{(criterion.levels || []).map(level => <div key={level.id || level.value} className={`p-3 ${Number(scores[criterion.id]) === level.value ? 'bg-cyan-50 ring-2 ring-inset ring-cyan-600' : 'bg-white'}`}><p className="text-lg font-black text-cyan-700">{level.value}</p><p className="mt-1 text-sm text-slate-600">{level.description || 'Sin descripción'}</p></div>)}</div></section>)}</div>{grade?.comments && <div className="mt-5 rounded-xl border border-amber-200 bg-amber-50 p-4"><p className="text-xs font-black uppercase text-amber-700">Retroalimentación</p><p className="mt-1 text-sm text-slate-700">{grade.comments}</p></div>}</main></div>;
}
