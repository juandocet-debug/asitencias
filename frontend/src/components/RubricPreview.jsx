import React from 'react';
import { Download, X } from 'lucide-react';

export default function RubricPreview({ rubric, onClose }) {
    return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/55 p-3 sm:p-6"><div className="flex max-h-[94vh] w-full max-w-5xl flex-col overflow-hidden rounded-2xl bg-white shadow-2xl"><header className="flex items-center justify-between bg-[#164e63] px-4 py-3 text-white"><div><p className="text-[10px] font-black uppercase tracking-widest text-cyan-200">Vista previa para estudiantes</p><h2 className="font-black">{rubric.title}</h2></div><button onClick={onClose} className="rounded-lg p-2 hover:bg-white/10"><X size={18} /></button></header><div className="overflow-auto bg-slate-100 p-4 sm:p-6"><div className="mx-auto max-w-4xl bg-white p-5 text-slate-900 shadow-sm sm:p-8"><h1 className="text-xl font-black">{rubric.title}</h1>{rubric.description && <p className="mt-2 text-sm text-slate-600">{rubric.description}</p>}<div className="mt-5 overflow-x-auto"><table className="w-full min-w-[720px] border-collapse text-sm"><thead><tr><th className="border border-slate-300 bg-slate-200 p-2 text-left">Criterio</th>{(rubric.criteria?.[0]?.levels || []).map(level => <th key={level.id || level.value} className="border border-slate-300 bg-slate-200 p-2 text-left">Nivel {level.value}</th>)}</tr></thead><tbody>{(rubric.criteria || []).map(criterion => <tr key={criterion.id}><td className="border border-slate-300 p-2 font-black">{criterion.name}</td>{(criterion.levels || []).map(level => <td key={level.id || level.value} className="border border-slate-300 p-2 align-top text-xs">{level.description || 'Sin descripción'}</td>)}</tr>)}</tbody></table></div></div></div><footer className="flex justify-end gap-2 border-t border-slate-100 bg-white p-3"><button onClick={() => window.print()} className="inline-flex items-center gap-2 rounded-lg bg-cyan-700 px-4 py-2 text-sm font-black text-white"><Download size={15} /> Descargar / imprimir</button></footer></div></div>;
}
